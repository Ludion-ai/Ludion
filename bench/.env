# Gate 2.7 (decisions F-2) — PASTE-POINT: after `wrangler deploy`, replace
# <account> with the real workers.dev subdomain (or a custom domain). While the
# placeholder is present the bench hides Submit and offers Download JSON only.
# Local dev against `wrangler dev`:
#   VITE_COLLECTOR_URL=http://localhost:8787 pnpm dev:bench
VITE_COLLECTOR_URL=https://ludion-collector.<account>.workers.dev
