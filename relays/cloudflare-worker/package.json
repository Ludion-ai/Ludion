{
  "name": "ludion-fallback-relay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "Ludion Workspace v1: canonical OpenAI-compatible fallback relay (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260612.1",
    "typescript": "5.9.3",
    "vitest": "4.1.8",
    "wrangler": "4.86.0"
  }
}
