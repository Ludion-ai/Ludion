# Copy to .dev.vars (gitignored) for `wrangler dev`.
# Obviously-fake dev values — real secrets are set with `wrangler secret put`.
IP_HASH_SALT = "dev-salt-not-secret"
ADMIN_TOKEN = "dev-admin-token"
ALLOWED_ORIGINS = "http://localhost:5173,https://localhost:5173,http://localhost:4173,https://localhost:4173"
