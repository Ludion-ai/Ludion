# Public read-only collector base (Gate 4 ①). The demo reads /v1/aggregate for the
# one-line crowd comparison; it never submits. Operator swaps this for their own
# deployment. While it contains "<account>" the comparison line is simply omitted.
VITE_COLLECTOR_URL=https://ludion-collector.ludion.workers.dev
