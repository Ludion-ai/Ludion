{
  "name": "entelic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "supplier-table": "pnpm --filter entelic-bench supplier-table",
    "dev:bench": "pnpm --filter entelic-bench dev",
    "dev:demo": "pnpm --filter entelic-demo dev"
  }
}
