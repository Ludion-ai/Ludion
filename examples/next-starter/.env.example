# OPTIONAL — only needed for the server fallback step (see README).
# The on-device first run needs none of this. Server-side only — never NEXT_PUBLIC_.
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=sk-...
